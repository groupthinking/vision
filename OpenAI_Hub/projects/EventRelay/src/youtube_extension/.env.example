# Sample env file for the FastAPI app (used with `uvicorn --env-file`).
# Copy to `src/youtube_extension/.env` and populate before running the server directly.

GEMINI_API_KEY=your-gemini-key
GOOGLE_API_KEY=${GEMINI_API_KEY}
YOUTUBE_API_KEY=your-youtube-key
VIDEO_PROCESSOR_TYPE=hybrid
DATABASE_URL=sqlite:///./.runtime/app.db
FASTVLM_REPO_PATH=/absolute/path/to/ml-fastvlm
FASTVLM_MODEL_PATH=/absolute/path/to/ml-fastvlm/checkpoints/llava-fastvithd_0.5b_stage3
PYTORCH_ENABLE_MPS_FALLBACK=1
