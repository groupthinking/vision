# MCP Configuration Fixes

- "Hugging Face" with key: `hf_your_hf_token`
- "hf-mcp-server" with key: `hf_your_hf_server_token`

Update the MCP configuration to reference secure environment variables rather than hard-coded tokens.
